name: market_place_car
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl_phone_field:
    path: 'intl_phone_field'

  flutter_bloc: ^9.1.1
  shimmer: ^3.0.0
  get_it: ^9.2.1
  shared_preferences: ^2.5.5
  dartz: ^0.10.1
  dio: ^5.9.2
  cached_network_image: ^3.4.1
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
  generate: true

  assets:
    - assets/images/
    - assets/icons/

